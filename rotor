#!/usr/bin/env python3
"""rotor — Linux launcher. All CLI logic lives in rotorcli.py (shared with the
Windows PyInstaller entry point)."""
import os
import sys

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))
from rotorcli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
